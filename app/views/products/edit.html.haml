= render partial: 'header'

.edit
  .edit__main
    .edit__main__title 製品情報編集
    = form_with(model: @product, url: product_path, local: true) do |f|
      .form-wrapper
        .form-name 製品名
        %div
          = f.text_field :name, class: "input-form"
      .form-wrapper
        .form-name 在庫切れ通知個数
        %div
          = f.number_field :low_quantity, type: "number", min: "1", max: "9999999", class: "input-form", required: true
      .btn-wrapper
        = f.submit '更新する'